# 웹 배포 방법

## 1. 프로젝트 배포 전 확인

현재 프로젝트는 Vite 기반 React 프론트엔드입니다.

배포 산출물은 아래 명령으로 생성되는 `dist` 폴더입니다.

```bash
npm install
npm run build
```

배포 전에 아래 명령이 통과하는지 확인합니다.

```bash
npm run lint
npm run build
```

## 2. 환경변수 설정

운영 배포에서는 개발 서버 프록시를 사용할 수 없으므로, 실제 백엔드 주소를 `VITE_API_BASE_URL`에 넣어야 합니다.

```env
VITE_API_BASE_URL=http://34.158.219.4:8080
```

현재 로컬 개발용 `.env`는 Vite 프록시를 쓰도록 아래처럼 되어 있을 수 있습니다.

```env
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://34.158.219.4:8080
```

이 설정은 로컬 개발 서버용입니다. 정적 배포 환경에서는 `VITE_API_PROXY_TARGET`가 동작하지 않습니다.

## 3. 백엔드 CORS 확인

운영 프론트 도메인에서 백엔드 API를 직접 호출하려면 백엔드에서 CORS 허용이 필요합니다.

백엔드에서 최소한 아래 항목을 허용해야 합니다.

- 프론트 운영 도메인 Origin
- `GET`, `POST`, `PUT`, `PATCH`, `DELETE`, `OPTIONS`
- `Content-Type`
- 쿠키 인증을 위한 `credentials: true`

이 프로젝트는 Spring Security 세션 기반 인증을 사용하므로 `JSESSIONID` 쿠키가 브라우저에 저장되고 이후 요청에 포함되어야 합니다.

## 4. Vercel 배포 방법

1. GitHub에 프로젝트를 push합니다.
2. Vercel에서 새 프로젝트를 생성하고 해당 저장소를 연결합니다.
3. Framework Preset은 `Vite`를 선택합니다.
4. Build Command는 아래와 같이 설정합니다.

```bash
npm run build
```

5. Output Directory는 아래와 같이 설정합니다.

```text
dist
```

6. Environment Variables에 아래 값을 추가합니다.

```env
VITE_API_BASE_URL=http://34.158.219.4:8080
```

7. 배포 후 백엔드 CORS에 Vercel 배포 도메인을 추가합니다.

## 5. Netlify 배포 방법

1. GitHub 저장소를 Netlify에 연결합니다.
2. Build command를 아래로 설정합니다.

```bash
npm run build
```

3. Publish directory를 아래로 설정합니다.

```text
dist
```

4. Environment variables에 아래 값을 추가합니다.

```env
VITE_API_BASE_URL=http://34.158.219.4:8080
```

5. SPA hash 라우팅을 사용하므로 큰 리라이트 설정은 필요하지 않습니다. 단, 추후 BrowserRouter로 변경하면 `/* -> /index.html` 리라이트가 필요합니다.

## 6. Nginx 서버 배포 방법

1. 서버에서 빌드합니다.

```bash
npm install
npm run build
```

2. 생성된 `dist` 폴더를 Nginx 정적 파일 경로로 복사합니다.

```bash
sudo mkdir -p /var/www/gachonbridge
sudo cp -r dist/* /var/www/gachonbridge/
```

3. Nginx 설정 예시입니다.

```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/gachonbridge;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

4. 설정을 검사하고 재시작합니다.

```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 7. Nginx에서 API 프록시까지 같이 처리하는 방법

백엔드 CORS 설정을 피하고 싶다면, 프론트 도메인의 `/api`, `/image`를 백엔드로 프록시할 수 있습니다.

이 경우 프론트 환경변수는 비워둡니다.

```env
VITE_API_BASE_URL=
```

Nginx 설정 예시입니다.

```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/gachonbridge;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://34.158.219.4:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /image/ {
        proxy_pass http://34.158.219.4:8080/image/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

세션 쿠키 기반 로그인까지 안정적으로 쓰려면 프론트와 API가 같은 도메인처럼 보이는 이 방식이 가장 단순합니다.

## 8. 배포 후 확인할 것

- 로그인 성공 여부
- 최초 로그인 시 회원가입 화면 이동 여부
- 메인 대시보드 포트폴리오 목록 조회 여부
- 포트폴리오 작성 및 이미지 업로드 여부
- 마이페이지 내 포트폴리오 조회 여부
- 공감 토글 여부
- 새로고침 후 화면이 정상 표시되는지 여부

## 9. 참조 문서

- `Reference/사용자 요구사항 정의.md`
- `Reference/API 명세서.md`
- `Reference/DB 테이블 명세서.md`

시스템 아키텍처 md 파일은 현재 `Reference` 폴더에서 확인되지 않았습니다.
